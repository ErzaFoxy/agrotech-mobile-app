import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    marginVertical: 15,
  },
  text: {
    color: '#fff',
    fontSize: 15,
  },
  dots: {
    color: '#fff',
    fontSize: 15,
    width: 24,
    textAlign: 'left',
  },
});